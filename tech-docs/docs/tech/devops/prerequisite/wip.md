1. Install Docker Desktop in personal account and sign In to docker hub

2. VsCode

3. Install node js

4. Install python

5. Install git bash

6. Add a screenshot of the version to make sure everything is installed properly

   1. Add a warning saying exact version is not required but make sure its latest stable version

7. At least 8 core cpu and 16 gb ram

8. Add a summary to all the slides & add timer for exercise

# docker issuse https://www.docker.com/blog/docker-engine-version-29/

$env:DOCKER_API_VERSION=1.44
PS C:\Users\sathish.thangavel> echo $env.DOCKER_API_VERSION
PS C:\Users\sathish.thangavel> $env:DOCKER_API_VERSION= "1.44"
PS C:\Users\sathish.thangavel> echo $env.DOCKER_API_VERSION

# kubectl alias for minikube

<!-- function kubectl { minikube kubectl -- @args } -->

0. Add a architecture follow of github to AKS deployment & what will be covered
   in what section
   1. Section 1 talks about this
   2. Section 2 is about this
1. Build start to end like to story

   1. Say the evolution of deployment
   2. When to use what
   3. Why things are designed the way they are
